{
  "name": "video-collage-cli",
  "version": "1.0.0",
  "module": "src/index.ts",
  "type": "module",
  "bin": {
    "video-collage": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile video-collage",
    "test": "bun test",
    "typecheck": "tsc --noEmit --skipLibCheck"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
